# Plan for goal-cbaf95a83490

Target: `RELEASED`. Project types: application, documentation, monorepo, public-web, service.

Complexity: `high`. Recommended sefers: `2`. Lane: `sequential`. Model class: `frontier`.

## Discovered commands

- `python -m unittest discover` (.)
- `python -m unittest discover` (plugins/zanaat-pack/skills/slack-gif-creator)
- `python -m unittest discover` (tests/fixtures/projects/library)
- `python -m unittest discover` (tests/fixtures/projects/python-api)
