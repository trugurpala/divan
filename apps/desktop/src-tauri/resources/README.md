# Generated Desktop resources

`divan-project.pyz` is generated from the exact checked-out Divan commit during the Windows build. It is intentionally not committed as a binary artifact.

The Desktop backend also accepts `DIVAN_PROJECT_PYZ` during local development.
