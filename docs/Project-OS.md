# Project OS compatibility page

`Project OS` was the public name used through v0.16 for the supervised contract
installed into a target repository. Its stable commands, `.divan/` data,
DCS/DPS identifiers, receipts, and hashes remain unchanged.

The canonical v0.17 documentation is [Divan Project Contract](Project-Contract.md).
