"""Canonical, stdlib-only Divan runtime.

The public product is Divan.  Domain modules are declared in ``modules.json``;
the former ``company`` package is a compatibility surface, not a second
runtime.
"""

__all__ = ["kernel"]
